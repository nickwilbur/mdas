{
  "$comment": "Read-only MCP allowlist. CI fails if any tool name matches /^(create|update|cancel|delete|post|send)_/. Do not add write-capable tool names here.",
  "servers": {
    "zuora-mcp": {
      "baseUrl": "https://apisandbox.zuora.com/mcp",
      "transport": "http",
      "auth": "oauth2_client_credentials",
      "allowedTools": [
        "query_objects",
        "ask_zuora",
        "account_summary",
        "run_report"
      ]
    },
    "zuora-cerebro": {
      "baseUrl": "https://cerebro-mcp.corpdata.zuora.com/mcp",
      "transport": "http",
      "auth": "oauth2_authorization_code",
      "comment": "Discovery/IDE only. Production MDAS worker uses CEREBRO_API_TOKEN REST. Requires Cerebro Engage access on first connect.",
      "allowedTools": []
    },
    "glean-mcp": {
      "transport": "http",
      "auth": "bearer",
      "allowedTools": [
        "search",
        "chat",
        "read_document"
      ]
    }
  }
}
