/** CSE commentary considered stale after this many days (Red/Yellow). */
export const CSE_COMMENTARY_STALE_DAYS_RED_YELLOW = 14;

/** CSE commentary considered stale after this many days (Green). */
export const CSE_COMMENTARY_STALE_DAYS_GREEN = 30;

/** Generic adapter/source freshness threshold (hours). */
export const SOURCE_FRESHNESS_HOURS = 72;

export const DAY_MS = 86_400_000;
